language: rust

dist: xenial

rust:
  - stable
  - beta
  - nightly

matrix:
  allow_failures:
    - rust: nightly

before_install:
  - sudo apt-get update
  - sudo apt-get install libgeos-dev

script:
  - cargo build
